{
  "name": "elmergram",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "elm make ./src/Main.elm --optimize --output ./server/bot.js",
    "start": "node ./server/server.js",
    "start:dev": "yarn build && yarn start"
  },
  "private": true,
  "dependencies": {
    "node-fetch": "^2.3.0"
  }
}